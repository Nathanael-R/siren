# Performance records

Performance claims require a release or production-like device build. For each run record hardware, OS, build commit, scenario, tool, JS/UI frame rates, dropped frames, memory before/after, bottleneck, change, and re-measurement. Do not substitute development-mode results.
