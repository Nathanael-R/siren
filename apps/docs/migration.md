# Migration guidance

Run `siren-ui diff <component>` before updating. `update` protects locally modified files and reports core/dependency changes. Breaking registry releases include before/after API examples and manual steps. v0.1 intentionally has no automatic merge engine.
