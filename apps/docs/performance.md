# Performance

Waveforms bucket input to visible density. Live history is fixed-size. High-frequency visuals use Reanimated shared values and never call React state once per audio frame. Gesture recognition stays native while callback delivery is bounded.

The repository contains release-build scenarios, but no physical-device performance claims are published until measurements are recorded under `docs/performance/`.
