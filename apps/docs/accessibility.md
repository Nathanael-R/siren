# Accessibility

Siren components expose labels, roles, state, live announcements, adjustable waveform actions, safe touch targets, reduced-motion behavior, and non-gesture recording controls. Test VoiceOver and TalkBack on physical devices. Directional gestures mirror in RTL. Color is paired with labels or shape/state changes.
