# Styling

Components use neutral React Native styles with safe structural defaults. Customize root `style`, a small set of meaningful part styles, slots, or the copied source. No theme provider or NativeWind runtime is required. The optional NativeWind recipe demonstrates integration without shaping the base API.
