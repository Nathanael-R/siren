# Registry architecture

Registry source is canonical. Metadata validation, deterministic JSON generation, generated playground/docs copies, hosted-first CLI lookup, bundled fallback, installed hashes, and non-destructive update behavior are described in the repository [architecture record](https://github.com/siren-ui/siren/blob/main/docs/architecture/registry.md).
