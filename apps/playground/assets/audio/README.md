# Siren demo audio

These WAV files are original deterministic fixtures generated by `pnpm generate:audio` in the playground package. They use simple additive synthesis and contain no third-party recordings, voices, music, or licensed samples.

Their precomputed normalized amplitude envelopes live in `data/demo-audio.generated.ts` so the audible clip, waveform, and agent visualizer share one timeline.
